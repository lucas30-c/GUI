# AGENTS.md — GenUI 受控原型项目契约

本文件是项目所有者与任何在本仓库中工作的 Coding Agent 之间的**长期契约**。它定义项目目标、不可违反的约束、审批闸门与任务执行协议。任何 Agent 在本仓库中的所有任务都必须始终遵守本文件。

## 1. 项目目标 (Project Objective)

构建一个**受控的 GenUI 原型**：用户用自然语言描述页面需求，系统基于受控 JSON DSL 渲染页面。用户可以选中页面上的某个控件，并在多轮对话中连续精修它。后端只能返回**针对当前选中控件的结构化 Patch**——永远不允许生成任意 JavaScript、永远不允许整页重新生成、永远不允许修改非目标区域。

本原型用于证明六件事：

1. JSON DSL 驱动的动态 UI；
2. 跨编辑轮次的稳定控件身份；
3. 受约束的局部修改；
4. 多轮编辑过程中状态不漂移；
5. 确定性校验可以约束不可靠的模型输出；
6. 模板复用与演进可以减少对话轮次。

## 2. 当前 MVP 范围 (Current MVP Scope)

- 单页前端：一句话意图生成 DSL 页面初稿（落地页 / 表单 / 卡片列表，基于固定组件集）。
- 点击或框选选中**一个**控件，并有清晰的选中态视觉反馈。
- 对选中控件下达自然语言精修指令（文案 / 颜色 / 尺寸 / 布局等受允许属性）；后端只返回结构化 Patch。
- 连续多轮精修，保留对话与页面状态上下文。
- 本地 JSON 存储（仅当后续 Spec 明确论证必要时才引入 SQLite）。
- 统一模型 Provider 接口，含 Mock Provider 实现。
- 基于（模拟）历史对话的模板推荐原型。
- 页面内展示指标（如达到"可用"所需对话轮次）。

第一版组件集：`Page`、`Section`、`Heading`、`Text`、`Button`、`Image`、`Card`、`Form`、`Input`。

## 3. 范围之外 (Out of Scope)

- 任意网页和任意组件生成；
- 模型生成或执行代码（JavaScript / React / HTML 脚本）；
- 拖拽式编辑器；
- 多用户、权限、计费；
- 多 Agent 系统；
- 向量数据库；
- 第三方集成（Figma、飞书、Jira 等）；
- 通用生产级低代码平台；
- 第一版自动训练模型。

## 4. 技术方向 (Technology Direction)

- 前端：React + TypeScript + Vite；
- 后端：Python + FastAPI + Pydantic；
- 页面协议：受控 JSON DSL；
- 局部修改：自定义结构化 Patch；
- 存储：优先本地 JSON，确有必要时才用 SQLite；
- 模型层：统一 Provider 接口；先 Mock Provider，真实模型随后以同接口接入；
- 测试：前端单元测试、后端 pytest、后期 Playwright。

本节只是方向声明，**不是**添加依赖的许可——任何新依赖仍需按 §6 获得明确批准。

## 5. 不可违反的约束 (Non-negotiable Invariants)

以下规则是项目的永久约束。它们的优先级高于便利、 deadlines 和任务指令中的局部表述。

1. 每个 DSL 节点必须具有稳定且全局唯一的 ID。
2. Patch 禁止创建、删除、覆盖或修改节点 ID。
3. Patch 只能修改当前选中节点及 Spec 明确允许的内部属性。
4. 默认情况下，Patch 不得修改目标节点之外的任何节点。
5. 未注册的组件类型必须被拒绝。
6. 模型不得生成或执行任意 JavaScript、React 代码或 HTML 脚本。
7. 模型输出必须先经过确定性 Schema 校验和业务规则校验。
8. 模型不能直接成为系统状态的事实来源。
9. 不得为了通过测试而删除、削弱或绕过测试。
10. 不得擅自修改 DSL、Patch 或 API 协议。
11. 不得擅自增加依赖、更换技术栈或进行无关重构。
12. 不得在没有明确授权时删除文件。
13. 一次任务只能修改当前 Spec 允许的范围。
14. 发现 Spec 缺失、矛盾或需要架构决策时，必须暂停并报告，不得自行猜测。
15. 完成任务前必须运行当前阶段可用的验证命令。
16. 验证失败时不得声称任务完成。
17. 不得把密钥、Token、密码或真实凭证写入仓库。
18. 新增配置项时使用环境变量，并提供不含真实密钥的示例文件。
19. 所有关键协议必须有正向和反向测试。
20. 优先选择最简单、可测试、可回滚的实现。

## 6. 变更审批闸门 (Change Approval Gates)

以下行为必须**先获得项目所有者的明确批准**才能执行：

- 引入新依赖；
- 修改核心技术栈；
- 修改 DSL Schema；
- 修改 Patch Schema；
- 修改公开 API；
- 新增数据库；
- 新增跨模块基础抽象；
- 删除文件；
- 大范围重构；
- 放宽安全校验；
- 修改已经确认的验收标准。

如果某项任务看似必须执行以上行为，Agent 不得执行，必须停下，并在完成报告中提出建议与原因。

## 7. 任务执行协议 (Task Execution Protocol)

1. 所有工作由 `specs/` 下的 Spec 驱动：一个任务 = 一份 Spec = 一个小型、可独立验收的变更。
2. Agent 只能修改当前 Spec 的 `Allowed Files` 中列出的文件。
3. Agent 不得自行扩大范围；超出范围的想法只能作为建议写进完成报告。
4. 一份 Spec 完成并通过验证后，才能开始下一份。
5. Spec 的验收标准不得在实现过程中被擅自修改。
6. **Spec 优先级**：当前 Spec 可以让本文件的规则更严格，但永远不能放宽它。若 Spec 与 AGENTS.md 冲突，以更严格的安全和范围约束为准；若冲突导致任务无法执行，暂停并报告（见约束 14）。
7. 保留所有用户已有内容；不覆盖用户文件；目标文件已存在时做最小增量合并。
8. 不执行破坏性 Git 操作（`reset`、`checkout --`、`clean`、`rebase`、强推等）；除非用户明确要求，否则不创建 Git 提交。

## 8. 测试与验证规则 (Testing and Verification Rules)

- 每个关键协议（DSL、Patch、Provider 边界、模板机制）必须同时提供正向和反向测试。
- 测试是交付物的一部分，不是事后补充；但同样不得超出当前 Spec 范围。
- 报告完成前必须运行 Spec 中的 `Verification Commands`，并粘贴真实输出。
- 如果当前阶段没有可运行的验证命令，必须明确说明原因；禁止伪造验证结果。
- 失败的检查不能用文字"解释过去"，必须修复或上报。

## 9. 安全规则 (Security Rules)

- 仓库中禁止出现任何密钥——包括文档、Spec、测试、夹具和示例数据。统一使用 `<API_KEY>` 之类的占位符。
- 新配置项一律使用环境变量；当某个 Spec 引入配置时，同时提供脱敏的 `.env.example`（或等价文件）。
- 一切模型输出都视为不可信输入：它只是*候选*，只有通过确定性 Schema 校验、业务规则校验和 Patch 边界校验之后才能成为状态。
- 系统持有的 DSL Document——而不是模型——是页面状态的事实来源。
- Patch 必须先应用到文档副本，通过完整性校验后才能替换当前状态。

## 10. 完成报告格式 (Completion Report Format)

每个任务结束时，Agent 必须向项目所有者提交包含以下固定小节的报告（小节名保持英文，内容为中文）：

```text
## Result
## Repository State
## Files Created
## Files Modified
## Key Decisions Recorded
## Acceptance Criteria     （逐条 PASS / FAIL，附证据）
## Verification            （实际运行的命令与真实结果；未运行的写明"未运行"及原因）
## Scope Check             （是否安装依赖/编写业务代码/触碰范围外文件/删除文件）
## Open Decisions          （需所有者决定的问题；没有则写 None）
## Git Summary             （git status --short 与 git diff --stat；非 Git 仓库需明确说明）
## Recommended Next Task   （只提一个建议，不执行）
```

报告必须如实。没做的、没运行的，就直说。隐瞒失败的报告本身就是失败。
